name: "Nightly - Common"

on:
  workflow_call:
    inputs:
      platform:
        type: string
        required: true
        description: "Target platform to build for"
      args:
        type: string
        required: false
        description: "Additional arguments passed to tauri-action"
        default: ""
      tag:
        type: string
        required: false
        description: "Tag to use for nightly builds"
        default: "rolling"
      build-counter-file:
        type: string
        required: true
        description: "File to use for build counter"

jobs:
  check-for-changes:
    outputs:
      changed: ${{ steps.detect-changes.outputs.changed }}

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - uses: ./.github/actions/detect-changes
        id: detect-changes

  prepare:
    needs: check-for-changes
    if: needs.check-for-changes.outputs.changed == 'true'

    permissions:
      contents: write

    outputs:
      build-number: ${{ steps.compute-counter.outputs.build-number }}
      date: ${{ steps.date.outputs.date }}

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Compute nightly build counter
        id: compute-counter
        shell: bash
        run: |
          CURRENT=$(cat ${{ inputs.build-counter-file }})
          NEXT=$((CURRENT + 1))
          echo "build-number=$NEXT" >> "$GITHUB_OUTPUT"

      - name: Get current date
        id: date
        shell: bash
        run: echo "date=$(date '+%Y-%m-%d')" >> "$GITHUB_OUTPUT"

      - name: Delete existing ${{ inputs.tag }} release and tag
        uses: actions/github-script@v9
        with:
          script: |
            const tag = '${{ inputs.tag }}';

            // delete release if exists
            try {
              const { data } = await github.rest.repos.getReleaseByTag({
                owner: context.repo.owner,
                repo: context.repo.repo,
                tag,
              });

              await github.rest.repos.deleteRelease({
                owner: context.repo.owner,
                repo: context.repo.repo,
                release_id: data.id,
              });
            } catch (e) {
              if (e.status !== 404) throw e;
            }

            // delete tag reference if exists
            try {
              await github.rest.git.deleteRef({
                owner: context.repo.owner,
                repo: context.repo.repo,
                ref: `tags/${tag}`,
              });
            } catch (e) {
              if (e.status !== 404 && e.status !== 422) throw e;
            }

  nightly:
    needs: prepare

    permissions:
      contents: write

    uses: ./.github/workflows/tauri-build.yaml
    secrets: inherit
    with:
      platform: ${{ inputs.platform }}
      args: ${{ inputs.args }}
      nightly: true
      build-number: ${{ needs.prepare.outputs.build-number }}
      tagName: ${{ inputs.tag }}
      releaseName: "Quark Nightly ${{ needs.prepare.outputs.date }}"
      releaseBody: "Automated nightly build from main"

  finalize:
    needs:
      - prepare
      - nightly

    permissions:
      contents: write

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
        with:
          token: ${{ secrets.QUARK_ACCESS_TOKEN }}
          ref: ${{ github.ref_name }}

      - name: Commit nightly build counter after successful build
        env:
          BUILD_NUMBER: ${{ needs.prepare.outputs.build-number }}
        run: |
          COUNTER_FILE='${{ inputs.build-counter-file }}'

          git config user.name '${{ github.actor }}'
          git config user.email '${{ github.actor }}@users.noreply.github.com'

          printf '%s\n' "$BUILD_NUMBER" > "$COUNTER_FILE"

          # do not update if there is no diff
          if git diff --quiet -- "$COUNTER_FILE"; then
            exit 0
          fi

          git add "$COUNTER_FILE"
          git commit -m "ci: bump nightly build counter to $BUILD_NUMBER"
          git push origin "HEAD:${GITHUB_REF_NAME}"
