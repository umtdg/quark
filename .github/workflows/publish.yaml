name: 'publish'
description: 'Create a release build'

on:
  push:
    tags:
      - 'v*'

jobs:
  publish:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'macos-latest'
            args: '--target aarch64-apple-darwin'
          - platform: 'ubuntu-22.04'
            args: ''
          - platform: 'windows-latest'
            args: ''

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v6

      - name: check version
        shell: bash
        run: |
          TAG="${GITHUB_REF_NAME#v}"
          PKG=$(jq -r '.version' package.json)
          CARGO=$(grep -m1 '^version' src-tauri/Cargo.toml | sed 's/.*"\(.*\)"/\1/')
          TAURI=$(jq -r '.version' src-tauri/tauri.conf.json)

          MISMATCH=0
          [ "$PKG" != "$TAG" ] && echo "package.json: $PKG != $TAG" && MISMATCH=1
          [ "$CARGO" != "$TAG" ] && echo "Cargo.toml: $CARGO != $TAG" && MISMATCH=1
          [ "$TAURI" != "$TAG" ] && echo "tauri.conf.json: $TAURI != $TAG" && MISMATCH=1

          [ "$MISMATCH" -eq 1 ] && exit 1
          echo "All versions match the tag $TAG"

      - uses: ./.github/actions/setup
        with:
          platform: ${{ matrix.platform }}

      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: v__VERSION__
          releaseName: 'Quark v__VERSION__'
          releaseBody: 'See the assets to download this version and install'
          releaseDraft: false
          prerelease: false
          generateReleaseNotes: true
          args: ${{ matrix.args }}

